/// Permission types for contacts access.
enum PermissionType {
  /// Read-only access to contacts.
  read,

  /// Write-only access to contacts (rarely used alone).
  write,

  /// Both read and write access.
  readWrite,
}
